apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
  namespace: training #You can add namespace to tell k8s on where to deploy it
spec:
 containers:
   - name: my-first-container
     image: nginx